name: Publish Internal Android Debug APK

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "frontend/**"

permissions:
  contents: write

jobs:
  build-and-publish-debug-apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Grant execute permission for gradlew
        run: chmod +x frontend/gradlew

      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Generate version
        id: version
        run: |
          DATE=$(date +%Y%m%d)
          VERSION_NAME="v${DATE}-${{ github.run_number }}"
          echo "version=${VERSION_NAME}" >> "$GITHUB_OUTPUT"
          echo "VERSION_NAME=${VERSION_NAME}" >> "$GITHUB_ENV"
          echo "VERSION_CODE=${{ github.run_number }}" >> "$GITHUB_ENV"

      - name: Setup Debug Keystore
        run: |
          mkdir -p frontend/androidApp
          echo "${{ secrets.DEBUG_KEYSTORE_BASE64 }}" | base64 -d > frontend/androidApp/debug.keystore

      - name: Setup google-services.json
        run: |
          echo "${{ secrets.GOOGLE_SERVICES_JSON_BASE64 }}" | base64 -d > frontend/androidApp/google-services.json

      - name: Build internal debug APK
        run: ./gradlew :androidApp:assembleDebug
        working-directory: frontend
        env:
          VERSION_NAME: ${{ env.VERSION_NAME }}
          VERSION_CODE: ${{ github.run_number }}
          KEYSTORE_PASSWORD: ${{ secrets.DEBUG_KEYSTORE_PASSWORD }}

      - name: Rename APK with version
        run: |
          mv frontend/androidApp/build/outputs/apk/debug/androidApp-debug.apk \
             frontend/androidApp/build/outputs/apk/debug/plexus-debug-${{ env.VERSION_NAME }}.apk

      - name: Publish pre-release with debug APK
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.version }}
          name: Plexus Internal Debug APK (${{ steps.version.outputs.version }})
          body: |
            Internal debug APK for device testing.
            This artifact is not intended for production distribution.
          files: frontend/androidApp/build/outputs/apk/debug/plexus-debug-*.apk
          generate_release_notes: true
          prerelease: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
