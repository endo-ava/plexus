# Gateway 環境変数設定例

# サーバー設定
GATEWAY_HOST=0.0.0.0
GATEWAY_PORT=8001

# 認証APIキー（必須、32バイト以上推奨）
GATEWAY_API_KEY=your-gateway-api-key-min-32-chars

# Webhook シークレット（必須、32バイト以上推奨）
GATEWAY_WEBHOOK_SECRET=your-webhook-secret-min-32-chars

# CORS設定（カンマ区切り）
CORS_ORIGINS=*

# ロギングレベル
LOG_LEVEL=INFO

# FCM プロジェクト ID（オプション、プッシュ通知を使用する場合）
# GCM_PROJECT_ID=your-fcm-project-id

# デフォルトユーザー ID（MVP 固定値）
DEFAULT_USER_ID=default_user

# セッション名パターン（正規表現）
SESSION_PATTERN=^agent-[0-9]{4}$
